import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // When you replace placeholder gradients with real CDN images, add domains here.
    remotePatterns: [],
  },
};

export default nextConfig;
